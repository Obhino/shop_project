{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}

    <header>
        <ul>
            <li><a href="{{ path('app_register') }}" >Inscription </a></li>
            <li> <a href="{{ path('app_login') }}"> Connexion</a></li>
            {% if app.user %}
                <li><a href="{{ path('new_shop') }}" >Créer une Boutique</a></li>
            {% endif %}
        </ul>
    </header>

    <div>
        <h1>Cette route n'appartient à aucun(e) utilisateur. souhaitez vous créer une boutique ici?</h1>
        <a href="{{ path('new_shop') }}" >Créer une Boutique</a>

    </div>
{% endblock %}
